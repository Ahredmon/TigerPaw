<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Allow plain-text HTTP to the local AI server on the host machine.
        192.168.0.134 is the LAN IP of the machine running LocalAI.
        10.0.2.2 is kept as a fallback for AVD emulator sessions.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.0.134</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
